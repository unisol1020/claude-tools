{
  "//": "The visual/functional Claude Code settings from this setup. install.sh MERGES these keys into ~/.claude/settings.json (it does NOT overwrite the file). __HOME__ is replaced with your home dir. NOTE: permissions.defaultMode is intentionally NOT here — bypassPermissions auto-approves every tool call and is opt-in only (see README + install.sh --bypass).",

  "statusLine": { "type": "command", "command": "bash __HOME__/.claude/statusline.sh" },
  "theme": "dark",
  "tui": "fullscreen",
  "effortLevel": "xhigh",
  "model": "opus[1m]",
  "autoCompactEnabled": true
}
